import 'package:flutter/material.dart';

class PolicePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shopping Page'),
      ),
      body: Center(
        child: Text('Welcome to Shopping Page!'),
      ),
    );
  }
}
