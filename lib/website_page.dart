import 'package:flutter/material.dart';

class WebsitePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shopping Page'),
      ),
      body: Center(
        child: Text('Welcome to web Page!'),
      ),
    );
  }
}

